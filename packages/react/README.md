# React Package

This package contains the first-party React integration for `tilt-to-edit`.

Implemented responsibilities:

- `useTiltToEdit` hook
- `TiltStepper`, `TiltSlider`, and `TiltListNavigator`
- React-focused examples of permission and calibration flows
