# Packages

This directory contains the library packages shipped in `v0.2.0`.

Implemented packages:

- `core`: sensor backends, calibration, normalization, diagnostics, simulator support, and edit-intent engine
- `react`: `useTiltToEdit` and first-party React components built on the core package
