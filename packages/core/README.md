# Core Package

This package contains the framework-agnostic tilt engine.

Implemented responsibilities:

- sensor backend abstraction
- permission and capability diagnostics
- calibration and screen-orientation normalization
- filtering, hysteresis, rate limiting, and confirmation state
- semantic edit-intent output and simulator support
