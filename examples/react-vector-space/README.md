# React Vector Space Example

This example visualizes tilt intent in a 3D chamber:

- the orb moves through a tilted spatial field
- the center beam shows current intent
- the velocity beam glows based on motion direction and speed
- the trail keeps the most recent motion history visible
