# React Stepper Example

Purpose:

- demonstrate discrete value changes driven by left and right tilt
- apply dead zones and repeat-rate limits
- require an explicit confirm action before committing a final value

This example should validate the safety model described in the ADRs.
