# React Basic Example

Purpose:

- show the minimum React integration path
- request tilt permission from a user gesture
- calibrate the neutral pose
- display normalized x and y intent values

This example should become the first reference implementation when `packages/core` and `packages/react` exist.
