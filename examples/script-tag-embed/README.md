# Script Tag Embed Example

This example demonstrates the plain browser bundle from `@tilt-to-edit/embed`.

It proves two integration paths:

- declarative auto-mounting with `data-tilt-to-edit`
- host-page reactions driven by `tilt-to-edit:*` custom events

The public GitHub Pages route is `/embed/`.
